# datapai-stock-be — Stock domain dependencies
# Platform deps (datapai-streamlit) must be installed first:
#   pip install -r ../datapai-streamlit/requirements.txt

# Stock data providers
yfinance
feedparser

# AI Observability
openlit>=1.0.0

# Already in platform but listed for clarity
# fastapi, uvicorn, psycopg2-binary, pandas, requests — from platform

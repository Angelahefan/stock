# core/ — stock-be domain modules (exchange registry, etc.)
# Must be a regular package (not namespace) so it wins over /opt/datapai/core/
# in PYTHONPATH resolution inside Airflow containers after the stock/platform
# repo restructure.
